# Selected auth artwork

The accepted asset is `public/images/auth/sparkfeed-angled-workspace-v1.webp`, 1122 × 1402 pixels, displayed with `object-cover`. It uses the first angled-workspace prototype selected by the user.

The composition-matching prompts below document a separate retained alternative, `sparkfeed-workspace-v1.webp`. They do not describe the current selected image.

# Sparkfeed auth illustration

Generated with built-in ImageGen on 2026-09-15. Final asset: `public/images/auth/sparkfeed-workspace-v1.webp`.

Composition reference: Shadcn Space login-2.webp, 490 x 712. Product references: user-supplied Sparkfeed Home and article-grid screenshots. Brand correction reference: existing apple-touch-icon.png.

Generated dimensions: 1040 x 1512, within 0.06% of the source aspect ratio. WebP quality 90, without resizing. The existing auth panel and object-cover behavior are preserved.

## Initial prompt

Use case: precise-object-edit / compositing. Edit reference image 1 into a Sparkfeed product illustration. Image 1 (490x712 WebP) is the exact COMPOSITION AND ASPECT RATIO TARGET. Images 2 and 3 are Sparkfeed interface content references, not composition references. Output ONE portrait artwork at exactly 980 x 1424 pixels if possible, or the identical width:height ratio 490:712 (0.688202247). Do not output a square, 4:5, wide image or contact sheet. This will replace the first image in the same auth-page image panel.

LOCK THE COMPOSITION OF IMAGE 1: thin straight regular grid covers the background; one huge thin desktop application window at exactly the same tilt and perspective, upper-left corner about 14% from left and 21% from top, upper edge rising gently toward the right, left edge diagonally moving right toward the bottom. Main window intentionally extends through the RIGHT and BOTTOM image boundaries. Do NOT center the whole application window or fit it inside the canvas. Exactly ONE smaller horizontal floating UI card overlays it on the LEFT at x=6.5%, y=45.5%, approximately 49% image width and 19.5% image height. Exactly three little rounded-square line-icon tiles occupy the same positions as original: upper left x=8% y=8%, upper right x=65% y=7%, lower left x=17% y=77%. Thin borders, soft restrained shadows. NO floating balls/spheres, no extra panes, no stands, no large headline outside screen, no new decorative shapes.

CHANGE THE SUBJECT FROM SHADCN ANALYTICS TO SPARKFEED: The big window is Sparkfeed's dark charcoal UI closely following screenshot 2 Home dashboard. Window brand wordmark 'Sparkfeed'. Sidebar includes Home selected in violet #7055FF, All articles, Today, Favorites, Discover, Sources, Spark AI; folder group labels AI Research Labs, Cloud Platforms, Marketing & Growth. Main area shows Home heading, a compact 'Add a feed' and 'Discover sources' control row, the Sources list and a partial row of article cards at the bottom, using real screenshot content and thumbnails. Keep interface proportions faithful, and preserve recognisable clear major labels without packing invented tiny text. No user email or account identity, no personal workspace names. No demo badge, outdated update counts, 'needs a look', 'no data', 'Never' or source error statuses. Replace all analytics, money, revenue, bar charts, sales and projects entirely with actual feeds/article content.

The foreground floating card is a Sparkfeed 'Sources' card with three clean rows: 'OpenAI Blog', 'Google DeepMind', 'HubSpot Marketing', each with a small corresponding source icon and a trailing subtle three-dot menu. No statistics or status labels. Replace the three decorative icon tiles with an orange RSS/feed line icon at upper left, a blue folder line icon upper right, and a teal bookmark line icon lower left, preserving original tile positions and scale.

CHANGE ONLY THE OUTER BACKDROP COLOUR FAMILY: it must be a light pastel violet-grey #F0EDF8 with extremely soft neutral variation and a fine low-contrast violet-grey grid. NOT BLACK OR NAVY. The application window and floating card remain charcoal dark as in the source product screenshots. The contrast between the bright pastel backdrop and dark screen should be crisp but calm. Keep the screenshot-like understated compositing style of original image 1 rather than glossy toy-like 3D. No shadcnspace text anywhere. No browser comment marker, no login form, no watermarks, no palm-tree devtool badge. The frame tilt, foreground-card position, icon positions, screen crop and overall portrait ratio must match image 1 as closely as possible.

## Refinement prompt

Edit image 1 with only three precise corrections. Preserve its current portrait aspect ratio, exact pixel dimensions if possible (1040x1512), screen perspective, crop, grid, pastel backdrop, every position, lighting and all other composition details. Image 2 is the EXACT Sparkfeed brand-symbol reference: a purple vertical half-disc with an overlapping white/translucent orb, NOT a lightning bolt. (1) Replace only the purple circular lightning icon beside the top-left 'Sparkfeed' wordmark with that exact supplied split-disc/orb brand symbol, naturally sized to the same 42px-ish region. No lightning bolt, no circle enclosing it, no redesigned mark. Keep wordmark 'Sparkfeed'. (2) In the foreground Sources card, the 'Google DeepMind' row should use a BLUE DeepMind-style circular swirl icon instead of a multicolor Google G. (3) The large foreground app's Fresh today article currently has a Gemini thumbnail but the title and source beneath it refer to something else. Correct this ONE article to source 'DEEPMIND.GOOGLE', title 'Gemini Omni 1.1 Flash lets you build with more control'. Remove the small OpenAI label over that article image. All other layout, size and elements stay unchanged. No added objects. No new dark outer background. This is the final clean-up of a screenshot-based Sparkfeed auth-panel composition. Keep the original image 1 proportions and design exactly.
